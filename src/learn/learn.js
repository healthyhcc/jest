const add = (a, b) => {
  return a + b;
};

const subtract = (a, b) => {
  return a - b;
};

const multip = (a, b) => {
  return a * b;
};

const division = (a, b) => {
  return a / b;
};

const object = {
  name: 'cxk',
  hobby: '🏀',
}

module.exports = {
  add,
  subtract,
  multip,
  division,
  object
};
